# Sharper than the default: for a clip going into a PR or a bug report, where the reviewer needs
# to read the diff or the UI, not just see that something moved. On top of whatever settings.conf
# says.

crf = 18
