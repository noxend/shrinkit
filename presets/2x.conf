# The everyday one: twice as fast, on top of whatever settings.conf says.
# Every file in this folder becomes a right-click entry, named after the file.
# Copy this to make your own; anything you leave out keeps whatever settings.conf says.

speed = 2
