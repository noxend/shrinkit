# The plain one: it sets nothing, so a recording is handled exactly as settings.conf says.
# It exists so that "shrinkit: default" has a place in the right-click menu next to the others.
# Leave it empty, or put a line here to make it your own.
