# The smallest the file gets: heavy compression and a capped resolution, for when size matters
# more than sharpness. Use it with --preset tiny, or give it its own right-click entry:
#
#     shrinkit preset install tiny
#
# Copy this file to make your own. Anything you leave out keeps whatever settings.conf says.

crf = 32
max_height = 1080
